-- Итоги по суткам кабинета больше не показываются: директор сверяется с
-- Ads Manager по заявкам, а они теперь совпадают и без отдельной строки.
-- Таблица осталась бы мёртвым грузом и лишним запросом к Meta при каждой
-- синхронизации.
drop table if exists ad_account_days;
